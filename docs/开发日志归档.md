# 卡密验证系统开发日志归档

> 本文档记录项目开发过程中的关键决策、问题解决和功能实现

---

## 项目信息

| 项目 | 信息 |
|------|------|
| 项目名称 | 卡密验证系统 |
| 开发周期 | 2026-03-12 ~ 2026-03-14 |
| 技术栈 | FastAPI + SQLite/Supabase + HTML/CSS/JavaScript |
| 最终状态 | ✅ 开发完成 |

---

## 一、需求演进

### 1.1 原始需求

创建卡密验证系统，用户输入卡密后访问飞书内容，包含管理后台和前端用户页面。

### 1.2 需求变更

| 变更内容 | 原方案 | 最终方案 | 变更原因 |
|---------|--------|---------|---------|
| 隐藏"进入原应用"按钮 | URL参数控制 | CSS裁剪 | URL参数方案在飞书侧无效 |
| 博主在线状态 | 后端状态 | 前端模拟 | 简化架构，基于时段概率分布 |
| 退出按钮位置 | 独立按钮 | 融合在线图标 | 优化UI布局 |

---

## 二、关键决策

### 2.1 CSS裁剪隐藏"进入原应用"按钮

**问题**：飞书多维表格嵌入时，顶部显示"进入原应用"按钮，存在导流风险。

**方案尝试**：
- ❌ 方案1：URL参数 `?hideTitleBar=true` - 飞书侧不支持
- ✅ 方案2：CSS裁剪 `clip-path` - 完美解决

**最终实现**：
```css
.feishu-iframe {
  clip-path: inset(0 0 0 0);
  margin-top: -50px;
  height: calc(100% + 50px);
}
```

**最佳裁剪高度**：50px

### 2.2 博主在线状态模拟

**问题**：如何展示博主的在线状态？

**方案**：前端模拟，基于时段概率分布
- 工作时段 (9:00-18:00)：高概率在线
- 晚间时段 (18:00-23:00)：中等概率
- 深夜时段 (23:00-9:00)：低概率

**优势**：
- 不依赖后端状态
- 无需维护在线状态同步
- 用户体验自然

### 2.3 退出按钮融合设计

**设计**：退出按钮与在线人数图标融合
- 默认显示：博主状态 + 在线人数
- 点击展开：显示退出登录按钮

**优势**：
- UI更简洁
- 操作逻辑清晰

---

## 三、数据库变更

### 3.1 迁移脚本记录

| 迁移脚本 | 变更内容 | 执行时间 |
|---------|---------|---------|
| `001_add_analytics_fields.sql` | 添加统计分析字段 | 2026-03-12 |
| `002_add_link_name.sql` | 添加链接名称字段 | 2026-03-13 |
| `003_add_session_id.sql` | 添加会话ID字段 | 2026-03-14 |

### 3.2 Session ID 字段添加

**问题**：`/api/report/session` 接口报错 `no such column: session_id`

**解决方案**：
1. 创建迁移脚本 `003_add_session_id.sql`
2. 更新 `model.py` 添加 `session_id` 字段定义
3. 更新 `db_client.py` 表初始化脚本
4. 创建索引 `idx_access_logs_session_id`

---

## 四、问题解决记录

### 4.1 统计卡片数据逻辑

**问题**：统计卡片"有效卡密"和"已激活"定义不清

**解决方案**：
- **已售出**：销售状态为 `sold` 的卡密
- **已激活**：有设备绑定的卡密

### 4.2 Supabase 客户端不支持 `not_.in_()`

**问题**：激活状态筛选在 Supabase 客户端不支持 `not_.in_()` 方法

**解决方案**：使用应用层过滤处理 NULL 值问题

### 4.3 SQLite 客户端缺少逻辑运算方法

**问题**：SQLite 客户端缺少 `or_()` 和 `not_()` 方法

**解决方案**：
1. 创建 `SQLiteNotWrapper` 类
2. 扩展 `SQLiteClient` 添加 `or_()` 和 `not_()` 方法
3. 保持与 Supabase 客户端 API 一致

### 4.4 筛选下拉框选项数量动态更新（2026-03-15）

**问题**：点击筛选下拉选项后，所有下拉选项只显示1个选项

**根本原因**：
1. 服务启动时未加载 `.env.local` 环境变量，导致连接到云端 Supabase 而非本地 SQLite
2. 后端 API 为所有字段应用了相同的筛选条件，导致每个字段的选项都只反映当前筛选结果

**解决方案**：
1. 在 `src/main.py` 开头添加加载 `.env.local` 环境变量的代码
2. 重构 `/api/admin/cards/filter-options` API，为每个字段分别执行查询：
   - 每个字段排除自身的筛选条件
   - 其他字段保留当前筛选条件

**核心代码变更**：
```python
# 加载环境变量
from dotenv import load_dotenv
_env_local = os.path.join(_parent_dir, '.env.local')
if os.path.exists(_env_local):
    load_dotenv(_env_local)

# 为每个字段分别构建查询
def build_query(exclude: str = None):
    # 排除指定字段的筛选条件
    if status and exclude != 'status':
        query = query.eq('status', status)
    # ...
```

### 4.5 筛选下拉框选中值不显示（2026-03-15）

**问题**：用户选中销售渠道、过期时间、链接名称筛选框中的选项后，列表正确显示筛选结果，但选中的选项不会在筛选框上显示

**根本原因**：`updateSelectDropdown` 函数使用 CSS 选择器 `option[value="${currentValue}"]` 来查找选项并恢复选中值，但如果值包含特殊字符（如中文"淘宝"），CSS 选择器可能无法正确匹配

**解决方案**：改用遍历选项的方式恢复选中值

**核心代码变更**：
```javascript
// 恢复选中值（遍历选项查找匹配，避免 CSS 选择器特殊字符问题）
if (currentValue) {
    const options = select.options;
    for (let i = 0; i < options.length; i++) {
        if (options[i].value === currentValue) {
            select.value = currentValue;
            break;
        }
    }
}
```

---

## 五、功能完成清单

### 5.1 前端功能 ✅

- [x] 卡密验证页面
- [x] 飞书内容嵌入（CSS裁剪隐藏按钮）
- [x] 博主在线状态模拟
- [x] 协作者头像显示
- [x] 退出登录功能
- [x] 安全保护措施

### 5.3 系统检查与修复（2026-03-15） ✅

- [x] 操作日志页面缺少"导入卡密"类型 → 添加 `import_cards` 类型映射
- [x] 访问日志销售状态筛选分页不准确 → 改为数据库层面筛选
- [x] 统计卡片数据不准确 → 新增专用统计API `/api/admin/cards/stats`
- [x] 全面功能检查通过

### 5.4 功能完整清单

#### 卡密管理模块
- [x] 卡密列表展示（分页、排序、多条件筛选）
- [x] 批量生成（自定义前缀、数量、过期时间）
- [x] 自动导出CSV（包含所有字段）
- [x] 编辑功能（销售渠道、销售状态、飞书链接、过期时间等）
- [x] 激活状态显示（有效、已激活、已停用）
- [x] ID改为序号显示
- [x] 链接名称列状态标识（绿点=已配置，灰点=未配置）

#### 筛选系统
- [x] 多条件筛选（AND逻辑）
- [x] 动态选项更新
- [x] 过期时间分组
- [x] 飞书链接分组（空链接合并为"未设置"）

#### 批量操作
- [x] 批量设置渠道/状态/链接
- [x] 批量删除
- [x] 返回信息准确修复

#### 日志系统
- [x] 操作日志（含 `import_cards` 类型）
- [x] 访问日志（支持销售状态筛选）

#### 数据分析
- [x] 统计卡片（总数、已售出、已激活、已停用）
- [x] 趋势图（折线图）
- [x] 过期状态统计（柱状图）
- [x] 链接健康页面

#### 导入导出
- [x] 导出CSV
- [x] 导入卡密
- [x] 导入销售状态

---

## 六、最终状态

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 服务健康 | ✅ 正常 | `/health` 接口正常 |
| 前端页面 | ✅ 正常 | 用户验证页、管理后台均可访问 |
| API接口 | ✅ 正常 | 50+ 个接口正常工作 |
| 数据库 | ✅ 正常 | SQLite/Supabase 双模式支持 |
| 日志 | ✅ 无错误 | 无异常日志 |

---

## 七、项目归档

**项目状态**: ✅ 开发完成，功能完整，运行稳定

**最终检查日期**: 2026-03-15

**检查结论**: 卡密管理后台功能完整、稳定、运行正常，所有核心功能均已实现并通过测试。
- [x] 数据导出（CSV/XLSX）
- [x] 数据导入（销售状态、卡密信息）
- [x] 数据清理

### 5.3 后端API ✅

- [x] 卡密验证接口
- [x] 在线用户统计
- [x] 会话数据上报
- [x] 完整的管理接口（28个）

---

## 六、合规措施

### 6.1 已移除的敏感数据

| 数据项 | 移除原因 | 移除日期 |
|-------|---------|---------|
| IP地址 | 属于个人信息 | 2026-03-13 |
| User-Agent | 包含设备指纹 | 2026-03-13 |
| 设备类型 | 来源于UA | 2026-03-13 |

### 6.2 数据收集原则

- ✅ 最小必要原则
- ✅ 仅收集业务必需数据
- ✅ 不收集任何个人信息

---

## 七、项目交付

### 7.1 代码统计

| 指标 | 数值 |
|------|------|
| 核心代码文件 | 9 个 |
| 代码行数 | 10,770 行 |
| API接口 | 28 个 |
| 数据库迁移 | 3 个 |

### 7.2 测试结果

| 测试项 | 状态 |
|--------|------|
| 服务运行状态 | ✅ 通过 |
| Python语法检查 | ✅ 通过 |
| 前端验证页面 | ✅ 通过 |
| 管理后台功能 | ✅ 通过 |
| API接口测试 | ✅ 通过 |
| 数据库迁移 | ✅ 通过 |
| 日志健康检查 | ✅ 通过 |

---

*归档时间：2026-03-14*

---

## 八、后续更新记录

### 8.1 预览转化模式优化（2026-03-23）

**需求背景**：
- 管理后台增加卡种管理功能
- 预览页面需要与验证页面样式统一
- 模糊程度设置功能冗余，需要简化

**关键修改**：

| 修改项 | 说明 |
|--------|------|
| 移除模糊程度设置 | 删除管理后台模糊程度选项，前端统一使用 `blur(4px)` |
| 预览页面样式统一 | 登录框、吉祥物、文字与验证页面完全一致 |
| 预览链接参数修正 | `?preview=` 改为 `?card_type_id=` |
| preview_image_id 字段处理 | 数据库无此字段，改为通过 image_key 反向查找 |

**Bug修复**：

| 问题 | 解决方案 |
|------|---------|
| 预览页面验证成功后无法跳转 | `showSuccess` 函数添加 `previewPage.style.display = 'none'` |
| 退出按钮未处理预览页面 | 退出逻辑添加预览页面隐藏 |
| 预览背景图片放大导致边缘问题 | 移除 `transform: scale()` 后调整为 `scale(0.98)` |

**最终预览背景效果**：
```css
.preview-background {
  filter: blur(4px) brightness(0.85);
  transform: scale(0.98);
}
```

**API变更**：
- 新增 `/api/preview/{card_type_id}` - 根据卡种ID获取预览信息
- 新增 `/api/preview/by-link/{link_name}` - 根据链接名称获取预览信息
- 新增卡种管理相关API（CRUD、统计、批量生成等）
- 新增预览图片管理API（上传、删除、列表）

---

*更新时间：2026-03-23*
