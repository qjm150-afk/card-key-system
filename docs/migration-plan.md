# 卡密验证系统迁移方案

## 一、现状分析

### 1.1 当前系统架构
```
┌─────────────────────────────────────────────────────────┐
│                   当前架构                               │
│                                                         │
│   用户 → 扣子平台托管 → 扣子内置 PostgreSQL             │
│         (FastAPI)      (DATABASE_URL 自动注入)          │
│              ↓                                          │
│        飞书多维表格嵌入                                  │
└─────────────────────────────────────────────────────────┘
```

### 1.2 当前成本分析

| 成本项 | 费用 | 说明 |
|--------|------|------|
| **扣子平台托管** | 按用量计费 | 预估 46 万积分/月 ≈ ¥460/月 |
| **扣子内置数据库** | 包含在托管费用中 | 无额外费用 |
| **服务器维护** | 时间成本 | 扣子平台负责 |

### 1.3 关键发现：数据库依赖

⚠️ **重要**：你现在使用的是**扣子内置 PostgreSQL 数据库**

| 数据库类型 | 连接方式 | 迁移影响 |
|------------|----------|----------|
| 扣子内置 PostgreSQL | `DATABASE_URL` 自动注入 | **离开扣子就无法使用** |

**这意味着**：如果迁移到阿里云 FC 或腾讯云 SCF，**必须同时迁移数据库**！

### 1.4 当前系统功能

| 功能模块 | 状态 | 依赖 |
|----------|------|------|
| 卡密验证 | ✅ 核心功能 | 服务器 + 数据库 |
| 设备限制 | ✅ 最多 5 台设备 | 数据库 |
| 过期判断 | ✅ 三种过期模式 | 数据库 |
| 管理后台 | ✅ 完整功能 | 服务器 + 数据库 |
| 飞书嵌入 | ✅ CSS 裁剪 | 前端 |
| 预览图片 | ✅ 对象存储 | 存储 |

---

## 二、迁移方案对比

### 方案 A：飞书协作者方案（推荐 - 最低成本）

**核心思路**：完全放弃 Web 系统，使用飞书原生能力

```
┌─────────────────────────────────────────────────────────┐
│                飞书协作者架构                            │
│                                                         │
│   用户 → 飞书文档 → 申请权限（填写卡密）                 │
│                    ↓                                    │
│              飞书机器人自动审批                          │
│                    ↓                                    │
│            飞书多维表格（卡密存储）                       │
└─────────────────────────────────────────────────────────┘
```

**成本分析**：
| 项目 | 费用 | 说明 |
|------|------|------|
| 飞书机器人 | **¥0** | 企业版免费 |
| 飞书多维表格 | **¥0** | 免费额度充足 |
| 服务器 | **¥0** | 无需服务器 |
| **总成本** | **¥0/月** | 完全免费 |

**功能对比**：
| 功能 | 原 Web 系统 | 飞书协作者方案 | 影响 |
|------|------------|----------------|------|
| 卡密验证 | ✅ | ✅ | 无影响 |
| 过期判断 | ✅ | ✅ | 无影响 |
| 设备限制 | ✅ 最多 5 台 | ❌ 改为用户绑定 | ⚠️ 有影响 |
| 管理后台 | ✅ 自建 | ✅ 飞书多维表格 | 不同体验 |
| 预览图片 | ✅ 自建 | ⚠️ 需重新设计 | 需调整 |

**适合场景**：
- ✅ 预算有限，追求零成本
- ✅ 用户接受使用飞书账号
- ✅ 可以接受「一卡一用户」而非「一卡多设备」

---

### 方案 B：迁移到免费 Serverless 平台

**核心思路**：保留 Web 系统，迁移到免费平台

```
┌─────────────────────────────────────────────────────────┐
│              Serverless 架构                            │
│                                                         │
│   用户 → Cloudflare Workers/Vercel → Supabase          │
│              ↓                                          │
│        飞书多维表格嵌入                                  │
└─────────────────────────────────────────────────────────┘
```

**平台选择**：

| 平台 | 免费额度 | 适合场景 |
|------|----------|----------|
| **Vercel** | 10万请求/月 | 静态站点 + Serverless Functions |
| **Cloudflare Workers** | 10万请求/天 | 全球 CDN + 边缘计算 |
| **Railway** | $5/月额度 | 小型应用部署 |
| **Render** | 750小时/月 | Web Service |

**成本分析**（以 Cloudflare Workers 为例）：
| 项目 | 费用 | 说明 |
|------|------|------|
| Cloudflare Workers | **¥0** | 10万请求/天免费 |
| Supabase | **¥0** | 500MB 免费 |
| 域名 | ¥50-100/年 | 可选，也可用免费域名 |
| **总成本** | **¥0-10/月** | 接近免费 |

**适合场景**：
- ✅ 需要保留设备限制功能
- ✅ 需要自建管理后台
- ✅ 访问量在免费额度内

---

### 方案 C：混合方案（渐进式迁移）

**核心思路**：双轨运行，逐步迁移

```
┌─────────────────────────────────────────────────────────┐
│                 混合架构                                │
│                                                         │
│   新用户 → 飞书协作者方案（零成本）                       │
│   老用户 → 原 Web 系统（逐步停用）                        │
└─────────────────────────────────────────────────────────┘
```

**迁移步骤**：
1. 新建飞书协作者系统
2. 新卡密全部走飞书方案
3. 老卡密到期后自动迁移
4. 最终关闭 Web 系统

---

## 三、推荐方案：飞书协作者方案

### 3.1 为什么推荐这个方案？

| 维度 | 分析 |
|------|------|
| **成本** | 完全免费，零运营成本 |
| **维护** | 无服务器、无数据库维护 |
| **安全** | 飞书企业级安全 |
| **合规** | 不收集个人信息 |
| **体验** | 用户直接访问文档，更自然 |

### 3.2 迁移实施计划

#### 第一阶段：准备工作（1-2 天）

```
Step 1: 创建飞书多维表格
    ├── 卡密表（存储卡密信息）
    ├── 验证日志表（记录验证历史）
    └── 导入现有卡密数据

Step 2: 创建飞书应用
    ├── 企业自建应用
    ├── 配置权限（文档、多维表格、消息）
    └── 配置事件订阅

Step 3: 部署机器人
    ├── 选择飞书云函数（推荐）
    └── 或部署到免费 Serverless 平台
```

#### 第二阶段：数据迁移（半天）

使用本地卡密生成工具导出 CSV，导入飞书多维表格：

```bash
# 导出现有卡密
访问：http://localhost:5000/admin → 导出 → CSV

# 导入飞书多维表格
打开飞书多维表格 → 导入 → 选择 CSV 文件
```

#### 第三阶段：测试验证（半天）

```
1. 创建测试卡密
2. 使用测试账号访问文档
3. 申请权限并填写卡密
4. 验证自动审批是否正常
5. 测试过期卡密、已用卡密等场景
```

#### 第四阶段：切换上线（半天）

```
1. 通知用户新的访问方式
2. 更新分享链接
3. 监控运行状态
4. 原 Web 系统可以下线或保留作为备份
```

### 3.3 功能变更说明

| 原功能 | 新方案 | 说明 |
|--------|--------|------|
| **设备限制**（5台） | 用户绑定 | 改为「一卡一用户」，更简单 |
| **管理后台** | 飞书多维表格 | 直接在表格中管理 |
| **预览图片** | 文档封面 | 使用飞书文档自带封面 |
| **访问日志** | 验证日志表 | 记录在多维表格中 |

---

## 四、本地工具配合使用

### 4.1 工具定位

本地卡密生成工具（`tools/local-card-generator/index.html`）配合飞书协作者方案：

```
┌─────────────────────────────────────────────────────────┐
│               本地工具 + 飞书方案                        │
│                                                         │
│   1. 本地工具：生成卡密 → 导出 CSV                       │
│   2. 飞书多维表格：导入 CSV → 卡密生效                   │
│   3. 飞书机器人：自动审批用户访问                        │
└─────────────────────────────────────────────────────────┘
```

### 4.2 使用流程

```
Step 1: 在本地工具中创建卡种
    └── 只需输入名称，如"VIP资料库"

Step 2: 批量生成卡密
    ├── 选择卡种
    ├── 设置飞书链接
    ├── 设置过期时间
    └── 生成 → 自动下载 CSV

Step 3: 导入飞书多维表格
    ├── 打开飞书多维表格
    ├── 导入 → 选择 CSV
    └── 卡密立即可用
```

---

## 五、决策建议

### 5.1 如果你的访问量...

| 访问量 | 推荐方案 | 理由 |
|--------|----------|------|
| **< 100 次/天** | 飞书协作者方案 | 零成本，体验好 |
| **100-1000 次/天** | 飞书协作者 或 Serverless | 都可，看功能需求 |
| **> 1000 次/天** | Serverless + Supabase | 需要更稳定的服务 |

### 5.2 如果你需要设备限制功能...

| 需求 | 推荐方案 | 说明 |
|------|----------|------|
| **必须保留** | 方案 B（Serverless） | 飞书方案不支持设备限制 |
| **可以改为用户绑定** | 方案 A（飞书协作者） | 一卡一用户，更简单 |

---

## 六、成本对比总结

| 方案 | 月成本 | 维护成本 | 功能完整度 | 推荐度 |
|------|--------|----------|------------|--------|
| **A. 飞书协作者** | ¥0 | 极低 | 90% | ⭐⭐⭐⭐⭐ |
| B. Serverless | ¥0-10 | 低 | 100% | ⭐⭐⭐⭐ |
| C. 混合方案 | ¥0-200 | 中 | 100% | ⭐⭐⭐ |
| 原方案（Coze 托管） | ¥460+ | 中 | 100% | ⭐⭐ |

---

## 七、用户场景分析（已确认）

### 7.1 已确认信息

| 问题 | 用户回答 | 影响 |
|------|----------|------|
| 日均访问量 | **50-200 次/天** | 所有免费额度都够用 |
| 设备限制功能 | **不重要** | 可以改为用户绑定或完全去掉 |
| 用户飞书账号 | **大部分用户没有** | ⚠️ 关键影响因素 |
| 管理后台频率 | 待确认 | 影响后台方案选择 |

### 7.2 关键发现：用户无飞书账号

**这是一个重要问题！** 

| 场景 | 用户体验 | 影响 |
|------|----------|------|
| 飞书协作者方案 | 用户需要先注册飞书账号 | 🚫 增加使用门槛 |
| Web 系统方案 | 用户直接输入卡密即可 | ✅ 无额外门槛 |

**结论**：由于大部分用户没有飞书账号，**推荐保留 Web 系统方案**，但迁移到免费平台。

---

## 八、最终推荐方案：国内云平台迁移

### 8.1 关键问题：国内访问速度

| 平台 | 国内访问 | 问题 |
|------|----------|------|
| **Vercel** | 🐢 慢/无法访问 | 服务器在海外，被墙风险 |
| **Cloudflare** | 🐢 慢 | 部分节点在国内访问慢 |
| **阿里云 FC** | ⚡ 快 | 国内节点，访问稳定 |
| **腾讯云 SCF** | ⚡ 快 | 国内节点，访问稳定 |
| **轻量服务器** | ⚡ 快 | 完全自主控制 |

### 8.2 推荐方案对比

| 方案 | 月成本 | 国内访问 | 维护成本 | 推荐度 |
|------|--------|----------|----------|--------|
| **A. 阿里云函数计算 FC** | **¥0** | ⚡ 快 | 低 | ⭐⭐⭐⭐⭐ |
| **B. 腾讯云 SCF** | **¥0** | ⚡ 快 | 低 | ⭐⭐⭐⭐⭐ |
| **C. 阿里云轻量服务器** | **¥24-50** | ⚡ 快 | 中 | ⭐⭐⭐⭐ |
| D. Vercel（海外） | ¥0 | 🐢 慢 | 低 | ⭐⭐ |

---

## 八-A. 方案 A：阿里云函数计算 FC（推荐）

### 架构图

```
┌─────────────────────────────────────────────────────────┐
│         阿里云函数计算 FC + 数据库（需选择）             │
│                                                         │
│   用户 → 函数计算 FC → 数据库                          │
│         (国内节点)     (三选一，见下表)                 │
│              ↓                                          │
│        飞书多维表格嵌入                                  │
│                                                         │
│   用户体验：访问快，无需飞书账号                         │
│   月成本：¥0-50（取决于数据库选择）                      │
└─────────────────────────────────────────────────────────┘
```

### ⚠️ 数据库迁移方案（必选）

由于你使用的是**扣子内置数据库**，迁移时必须选择新的数据库：

| 数据库选项 | 月成本 | 延迟 | 迁移难度 | 推荐度 |
|------------|--------|------|----------|--------|
| **A1. Supabase** | **¥0** | 海外，稍慢 | 中等 | ⭐⭐⭐⭐⭐ |
| **A2. 阿里云 RDS PostgreSQL** | ¥0（试用3个月）→ ¥50+ | 国内，快 | 简单 | ⭐⭐⭐⭐ |
| **A3. Neon** | **¥0** | 海外，稍慢 | 中等 | ⭐⭐⭐⭐ |
| **A4. 轻量服务器 + PostgreSQL** | ¥24-50 | 国内，快 | 复杂 | ⭐⭐⭐ |

---

### 方案 A1：FC + Supabase（推荐，零成本）

```
┌─────────────────────────────────────────────────────────┐
│              FC + Supabase 架构                         │
│                                                         │
│   用户 → 阿里云 FC → Supabase PostgreSQL（海外）        │
│         (国内)        (免费 500MB)                      │
│                                                         │
│   总成本：¥0/月                                         │
│   数据库延迟：+100-300ms（可接受）                       │
└─────────────────────────────────────────────────────────┘
```

**免费额度**：

| 资源 | 免费额度 | 你的用量 | 费用 |
|------|----------|----------|------|
| FC 调用次数 | 100万次/月 | ~6000 次/月 | **¥0** |
| FC 执行时长 | 40万 GB-秒 | ~1000 GB-秒 | **¥0** |
| Supabase 存储 | 500MB | 预估 < 100MB | **¥0** |
| Supabase 带宽 | 5GB/月 | ~500MB/月 | **¥0** |
| **总成本** | - | - | **¥0/月** |

**迁移步骤**：

```
1. 注册 Supabase，创建 PostgreSQL 数据库
2. 导出扣子数据库数据
   python scripts/backup_data.py
   
3. 导入到 Supabase
   - 方式1：通过 Supabase Dashboard 导入 CSV
   - 方式2：使用脚本导入
   
4. 部署 FC，配置环境变量
   DATABASE_URL=postgresql://xxx@xxx.supabase.co:5432/postgres
   
5. 测试验证
```

---

### 方案 A2：FC + 阿里云 RDS（国内低延迟）

```
┌─────────────────────────────────────────────────────────┐
│              FC + 阿里云 RDS 架构                        │
│                                                         │
│   用户 → 阿里云 FC → 阿里云 RDS PostgreSQL              │
│         (国内)        (国内，低延迟)                    │
│                                                         │
│   总成本：¥0（试用3个月）→ ¥50+/月                      │
│   数据库延迟：< 10ms                                    │
└─────────────────────────────────────────────────────────┘
```

**RDS 定价（PostgreSQL，最便宜配置）**：

| 配置 | 月价格 | 说明 |
|------|--------|------|
| 基础版 1核1G | **¥50-80/月** | 无免费额度 |
| 高可用版 2核4G | ¥200+/月 | 生产环境推荐 |

**新用户优惠**：
- 首次购买 RDS 有 **免费试用 3 个月** 活动可用

**适合场景**：
- 需要最低延迟
- 预算允许 ¥50+/月
- 未来访问量会大幅增长

---

### 方案 A3：FC + Neon（零成本备选）

```
┌─────────────────────────────────────────────────────────┐
│              FC + Neon 架构                             │
│                                                         │
│   用户 → 阿里云 FC → Neon PostgreSQL（海外）            │
│         (国内)        (免费，Serverless)                │
│                                                         │
│   总成本：¥0/月                                         │
└─────────────────────────────────────────────────────────┘
```

**Neon 优势**：
- 完全免费，无需信用卡
- Serverless PostgreSQL，自动休眠
- 免费额度：0.5GB 存储，100万次查询/月

**与 Supabase 对比**：
| 对比项 | Supabase | Neon |
|--------|----------|------|
| 免费存储 | 500MB | 512MB |
| 免费查询 | 无限制 | 100万次/月 |
| 额外功能 | 认证、存储等 | 纯数据库 |
| 适合场景 | 需要更多功能 | 只需数据库 |

---

### 方案 A4：轻量服务器 + 本地 PostgreSQL（完全自主）

```
┌─────────────────────────────────────────────────────────┐
│           轻量服务器 + 本地 PostgreSQL                  │
│                                                         │
│   用户 → 轻量服务器 → 本地 PostgreSQL                  │
│         (国内)        (同一服务器)                      │
│                                                         │
│   总成本：¥24-50/月                                    │
│   数据库延迟：< 1ms（本地）                             │
└─────────────────────────────────────────────────────────┘
```

**优势**：
- 完全自主控制
- 数据库零延迟
- 无需担心免费额度

**劣势**：
- 需要自己维护数据库
- 需要自己备份

---

## 八-A-总结：数据库方案选择

| 方案 | 月成本 | 延迟 | 维护成本 | 推荐场景 |
|------|--------|------|----------|----------|
| **A1. Supabase** | **¥0** | 中等 | 低 | **追求零成本首选** |
| A2. 阿里云 RDS | ¥50+ | 低 | 低 | 需要最低延迟 |
| A3. Neon | **¥0** | 中等 | 低 | 只需数据库 |
| A4. 本地数据库 | ¥24-50 | 极低 | 中 | 完全自主控制 |

### 🏆 推荐组合

**零成本方案**：阿里云 FC + Supabase
- 总成本：**¥0/月**
- 功能完整，体验好

**低延迟方案**：阿里云 FC + 阿里云 RDS
- 总成本：¥50+/月
- 国内数据库，延迟最低

---

## 八-B. 方案 B：腾讯云 SCF（同样推荐）

### 免费额度

| 资源 | 免费额度 | 你的用量 | 费用 |
|------|----------|----------|------|
| 函数调用次数 | **40万次/月** | ~6000 次/月 | **¥0** |
| 函数执行时长 | 4万 GB-秒/月 | ~1000 GB-秒/月 | **¥0** |
| 外网出流量 | 1GB/月 | ~500MB/月 | **¥0** |
| **总成本** | - | - | **¥0/月** |

### 与阿里云对比

| 对比项 | 阿里云 FC | 腾讯云 SCF |
|--------|----------|------------|
| 免费额度 | 更大（100万次） | 够用（40万次） |
| 文档质量 | 完善 | 完善 |
| Python 支持 | ✅ | ✅ |
| 推荐度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**选择建议**：哪个账号方便用哪个，功能差异不大。

---

## 八-C. 方案 C：轻量应用服务器

### 如果你不信任 Serverless...

```
┌─────────────────────────────────────────────────────────┐
│              轻量应用服务器架构                          │
│                                                         │
│   用户 → 轻量服务器 → PostgreSQL/SQLite                │
│         (2核2G)     (或继续用 Supabase)                │
│              ↓                                          │
│        飞书多维表格嵌入                                  │
│                                                         │
│   用户体验：访问快，完全自主                             │
│   月成本：¥24-50                                        │
└─────────────────────────────────────────────────────────┘
```

### 价格对比

| 云服务商 | 配置 | 月价格 | 说明 |
|----------|------|--------|------|
| **阿里云轻量** | 2核2G | **¥24-35** | 新用户优惠 |
| **腾讯云轻量** | 2核2G | **¥25-40** | 新用户优惠 |
| 华为云 HECS | 2核2G | ¥30-50 | 略贵 |

### 适合场景

| 场景 | 是否适合 |
|------|----------|
| 需要完全自主控制 | ✅ 适合 |
| 不想依赖免费额度的限制 | ✅ 适合 |
| 预算允许 ¥24-50/月 | ✅ 适合 |
| 追求零成本 | ❌ 不适合 |

---

## 八-D. 方案对比总结

| 方案 | 月成本 | 国内访问 | 维护成本 | 适合人群 |
|------|--------|----------|----------|----------|
| **A. 阿里云 FC** | **¥0** | ⚡ 快 | 低 | 追求零成本 |
| **B. 腾讯云 SCF** | **¥0** | ⚡ 快 | 低 | 追求零成本 |
| **C. 轻量服务器** | ¥24-50 | ⚡ 快 | 中 | 需要完全自主控制 |
| ~~Vercel~~ | ¥0 | 🐢 慢 | 低 | ❌ 国内不推荐 |

---

## 九、详细实施步骤（阿里云 FC）

### 第一阶段：准备工作（1-2 小时）

#### Step 1: 注册账号

```
1. 注册阿里云账号
   https://www.aliyun.com
   - 完成实名认证（必须）
   - 开通「函数计算 FC」服务

2. 注册 Supabase 账号
   https://supabase.com
   - 使用 GitHub 账号登录（推荐）
   - 创建新项目
   - 记录数据库连接信息
```

#### Step 2: 创建 Supabase 数据库

```
1. Supabase Dashboard → New Project
2. 设置项目名称和数据库密码
3. 选择区域（推荐：Singapore，离中国较近）
4. 等待项目创建完成（约 2 分钟）
5. 获取数据库连接字符串：
   Settings → Database → Connection string → URI
   格式：postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
```

#### Step 3: 导出扣子数据库数据

```bash
# 方式1：使用现有备份脚本
python scripts/backup_data.py

# 输出文件
backups/backup_YYYYMMDD_HHMMSS.json

# 方式2：通过管理后台导出
# 访问管理后台 → 卡密管理 → 导出 → CSV
# 访问管理后台 → 卡种管理 → 导出 → CSV
```

#### Step 4: 创建 Supabase 数据表

**方式1：使用 SQL Editor（推荐）**

```sql
-- 在 Supabase SQL Editor 中执行

-- 1. 卡种表
CREATE TABLE card_types (
    id SERIAL PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    preview_image TEXT,
    preview_enabled BOOLEAN DEFAULT FALSE,
    status INTEGER DEFAULT 1,
    sort_order INTEGER DEFAULT 0,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE
);

-- 2. 卡密表
CREATE TABLE card_keys_table (
    id SERIAL PRIMARY KEY,
    key_value VARCHAR(50) UNIQUE NOT NULL,
    status INTEGER DEFAULT 1,
    card_type_id INTEGER REFERENCES card_types(id),
    feishu_url TEXT,
    feishu_password VARCHAR(100),
    link_name VARCHAR(100),
    expire_at TIMESTAMP WITH TIME ZONE,
    expire_after_days INTEGER,
    activated_at TIMESTAMP WITH TIME ZONE,
    max_devices INTEGER DEFAULT 5,
    devices TEXT,
    user_note VARCHAR(200),
    sale_status VARCHAR(20) DEFAULT 'unsold',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE
);

-- 3. 访问日志表
CREATE TABLE access_logs (
    id SERIAL PRIMARY KEY,
    card_key_id INTEGER REFERENCES card_keys_table(id),
    key_value VARCHAR(50) NOT NULL,
    success BOOLEAN DEFAULT FALSE,
    error_msg VARCHAR(200),
    access_time TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    session_id VARCHAR(64)
);

-- 4. 创建索引
CREATE INDEX ix_card_keys_key_value ON card_keys_table(key_value);
CREATE INDEX ix_card_keys_status ON card_keys_table(status);
CREATE INDEX ix_access_logs_key_value ON access_logs(key_value);
```

**方式2：导入 CSV 文件**

```
1. Supabase Dashboard → Table Editor
2. 创建新表，字段与原表一致
3. 使用 Import 功能导入 CSV
```

#### Step 5: 导入数据

```bash
# 如果使用 SQL 方式，可以直接在 SQL Editor 执行 INSERT

# 如果使用 CSV 导入：
# 1. 在管理后台导出各表的 CSV
# 2. 在 Supabase Table Editor 中导入
```

---

### 第二阶段：代码适配（2-3 小时）

#### 需要修改的文件

| 文件 | 修改内容 | 工作量 |
|------|----------|--------|
| `template.yml` | 新建 FC 配置文件 | 30 分钟 |
| `src/main.py` | 适配 FC 入口格式 | 30 分钟 |
| `requirements.txt` | 确认依赖完整 | 10 分钟 |
| 环境变量配置 | FC 环境变量设置 | 15 分钟 |

#### 环境变量配置

```yaml
# template.yml 中的环境变量
EnvironmentVariables:
  DATABASE_URL: postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres
  ADMIN_USERNAME: your_admin_username
  ADMIN_PASSWORD: your_admin_password
```

### 第二阶段：代码适配（2-3 小时）

#### 需要修改的文件

| 文件 | 修改内容 | 工作量 |
|------|----------|--------|
| `template.yml` | 新建 FC 配置文件 | 30 分钟 |
| `src/main.py` | 适配 FC 入口格式 | 30 分钟 |
| `src/storage/database/` | 数据库连接适配 | 30 分钟 |
| `requirements.txt` | 确认依赖完整 | 10 分钟 |
| 环境变量配置 | FC 环境变量设置 | 15 分钟 |

#### FC 入口适配示例

```python
# src/fc_main.py (新建文件)

from flask import Flask, request, jsonify
from main import app as fastapi_app
import json

# Flask 适配层
flask_app = Flask(__name__)

@flask_app.route('/', defaults={'path': ''}, methods=['GET', 'POST', 'PUT', 'DELETE'])
@flask_app.route('/<path:path>', methods=['GET', 'POST', 'PUT', 'DELETE'])
def catch_all(path):
    # 将请求转发给 FastAPI
    from starlette.testclient import TestClient
    client = TestClient(fastapi_app)
    response = client.request(
        method=request.method,
        url=f"/{path}",
        headers=dict(request.headers),
        data=request.get_data(),
        params=request.args.to_dict()
    )
    return response.content, response.status_code, dict(response.headers)

# FC 入口函数
def handler(event, context):
    from werkzeug.serving import run_simple
    # FC 会调用这个函数
    return flask_app(event, context)
```

#### template.yml 配置

```yaml
ROSTemplateFormatVersion: '2015-09-01'
Transform: 'Aliyun::Serverless-2018-04-03'
Resources:
  card-key-service:
    Type: 'Aliyun::Serverless::Service'
    Properties:
      Description: 卡密验证服务
      MemorySize: 512
      Timeout: 60
      Runtime: python3.9
      EnvironmentVariables:
        DATABASE_URL: ${DATABASE_URL}
        SUPABASE_URL: ${SUPABASE_URL}
        SUPABASE_KEY: ${SUPABASE_KEY}
        
    card-key-function:
      Type: 'Aliyun::Serverless::Function'
      Properties:
        Handler: src.fc_main.handler
        CodeUri: ./
        Runtime: python3.9
        Timeout: 60
        MemorySize: 512
        
      HttpTrigger:
        Type: HTTP
        Properties:
          AuthType: anonymous
          Methods:
            - GET
            - POST
            - PUT
            - DELETE
```

### 第三阶段：本地测试（1 小时）

```bash
# 使用 fun 本地测试
fun local start

# 测试验证
curl http://localhost:8000/health
curl http://localhost:8000/
curl http://localhost:8000/admin
```

### 第四阶段：部署上线（1 小时）

```bash
# 部署到阿里云 FC
fun deploy

# 部署成功后会输出访问地址
# 例如：https://xxxxxx.cn-hangzhou.fc.aliyuncs.com
```

### 第五阶段：配置自定义域名（可选）

```
1. 阿里云控制台 → 函数计算 → 域名管理
2. 添加自定义域名
3. 配置路由规则：/.* → card-key-service/card-key-function
4. 域名解析：添加 CNAME 记录
```

### 第六阶段：数据迁移（30 分钟）

**如果继续用 Supabase**：无需迁移数据

**如果迁移到阿里云数据库**：
```bash
# 1. 创建 RDS PostgreSQL 实例（有免费试用）
# 2. 导出数据
python scripts/backup_data.py

# 3. 导入数据
python scripts/import_data_to_production.py
```

---

## 十、腾讯云 SCF 实施步骤（类似）

腾讯云 SCF 的流程与阿里云 FC 类似，主要差异：

| 对比项 | 阿里云 FC | 腾讯云 SCF |
|--------|----------|------------|
| 部署工具 | `fun` 或 `s` | `scf` |
| 配置文件 | `template.yml` | `template.yaml` |
| 入口函数 | `handler(event, context)` | `main_handler(event, context)` |
| 控制台 | 阿里云控制台 | 腾讯云控制台 |

**腾讯云 SCF 配置示例**：

```yaml
# template.yaml
Resources:
  CardKeyService:
    Type: TencentCloud::Serverless::Namespace
    CardKeyFunction:
      Type: TencentCloud::Serverless::Function
      Properties:
        CodeUri: ./
        Type: Event
        Handler: src.scf_main.main_handler
        Runtime: Python3.9
        MemorySize: 512
        Timeout: 60
        Environment:
          Variables:
            DATABASE_URL: ${DATABASE_URL}
            SUPABASE_URL: ${SUPABASE_URL}
            SUPABASE_KEY: ${SUPABASE_KEY}
        Events:
          HttpTrigger:
            Type: HTTP
            Properties:
              AuthType: NONE
              Namespace: default
```

### 第五阶段：切换上线（1 小时）

```
1. 全面测试新系统
   ├── 用户验证页面
   ├── 管理后台功能
   ├── API 接口
   └── 飞书嵌入显示

2. 更新分享链接
   └── 原链接 → 新域名

3. 通知用户（如需要）

4. 关闭原 Coze 托管服务
   └── 停止计费
```

---

## 十一、风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| FC 免费额度超限 | 极低 | 服务暂停 | 监控用量，设置告警 |
| 数据库连接超时 | 中 | 请求失败 | 使用连接池，设置超时时间 |
| 冷启动延迟 | 中 | 首次访问慢 | 设置预留实例（付费）或接受 |
| 迁移过程数据丢失 | 中 | 数据丢失 | 提前备份，多次验证 |
| API 兼容性问题 | 低 | 功能异常 | 充分测试 |

### 冷启动问题说明

| 场景 | 延迟 | 说明 |
|------|------|------|
| 函数长时间未调用 | 1-3 秒 | Python 运行时初始化 |
| 函数刚被调用过 | < 100ms | 热启动，快速响应 |
| 访问量 50-200/天 | 偶有冷启动 | 可接受 |

**优化方案**：
- 免费方案：接受偶尔的冷启动延迟
- 付费方案：设置「预留实例」，消除冷启动（约 ¥30/月）

---

## 十二、时间规划

| 阶段 | 预计时间 | 说明 |
|------|----------|------|
| 准备工作 | 1 小时 | 注册账号、安装工具、导出数据 |
| 代码适配 | 2-3 小时 | 适配 FC 入口、配置文件 |
| 本地测试 | 1 小时 | 确保功能正常 |
| 部署上线 | 1 小时 | 部署 + 配置域名 |
| 数据迁移 | 30 分钟 | 如需要 |
| **总计** | **5-7 小时** | 可分多天完成 |

---

## 十三、成本对比最终版（含数据库）

### 13.1 零成本方案

| 方案 | 月成本 | 国内访问 | 数据库延迟 | 推荐度 |
|------|--------|----------|------------|--------|
| **FC + Supabase** | **¥0** | ⚡ 快 | 中等 | ⭐⭐⭐⭐⭐ |
| **FC + Neon** | **¥0** | ⚡ 快 | 中等 | ⭐⭐⭐⭐ |
| **SCF + Supabase** | **¥0** | ⚡ 快 | 中等 | ⭐⭐⭐⭐ |

### 13.2 付费方案（更低延迟）

| 方案 | 月成本 | 数据库延迟 | 推荐场景 |
|------|--------|------------|----------|
| FC + 阿里云 RDS | ¥50+ | < 10ms | 需要最低延迟 |
| 轻量服务器 + 本地DB | ¥24-50 | < 1ms | 完全自主控制 |

### 13.3 与原方案对比

| 方案 | 月成本 | 用户门槛 | 功能完整度 |
|------|--------|----------|------------|
| **FC + Supabase（推荐）** | **¥0** | 无 | 100% |
| 轻量服务器 + 本地DB | ¥24-50 | 无 | 100% |
| ~~Vercel~~ | ¥0 | 无 | 100% |
| ~~飞书协作者~~ | ¥0 | **需要飞书账号** | 90% |
| 原 Coze 托管 | ¥460+ | 无 | 100% |

### 13.4 年度成本对比

| 方案 | 月成本 | 年成本 | 节省 |
|------|--------|--------|------|
| 原 Coze 托管 | ¥460 | ¥5520 | - |
| **FC + Supabase** | **¥0** | **¥0** | **¥5520** |
| 轻量服务器 | ¥35 | ¥420 | ¥5100 |

**结论**：迁移到 FC + Supabase，**每年节省 ¥5520**！

---

## 十四、我的建议

### 🏆 最终推荐：阿里云 FC + Supabase

**组合优势**：

| 优势 | 说明 |
|------|------|
| ✅ **完全免费** | FC 免费额度 + Supabase 免费额度 |
| ✅ **国内访问快** | FC 国内节点，延迟 < 50ms |
| ✅ **功能完整** | 100% 保留原有功能 |
| ✅ **用户无门槛** | 不需要飞书账号 |
| ✅ **维护简单** | Serverless，无需运维 |

**唯一缺点**：数据库在海外，有 100-300ms 额外延迟

**是否可接受？**：
- 当前系统：扣子 → 扣子数据库（都在海外）→ 延迟已存在
- 新系统：FC → Supabase（FC 国内，DB 海外）→ 延迟类似
- **结论：延迟体验不会变差**！

### 迁移时间规划

| 阶段 | 时间 | 内容 |
|------|------|------|
| 注册账号 | 30 分钟 | 阿里云、Supabase |
| 数据迁移 | 1-2 小时 | 导出 → 导入 |
| 代码适配 | 2-3 小时 | FC 配置、环境变量 |
| 部署测试 | 1 小时 | 部署 + 验证 |
| **总计** | **5-7 小时** | 可分多天完成 |

---

## 十五、决策点

### ❓ 问题 1：选择哪个计算平台？

| 选项 | 月成本 | 推荐度 |
|------|--------|--------|
| **A. 阿里云 FC** | ¥0 | ⭐⭐⭐⭐⭐ 推荐 |
| B. 腾讯云 SCF | ¥0 | ⭐⭐⭐⭐ |

### ❓ 问题 2：选择哪个数据库？

| 选项 | 月成本 | 延迟 | 推荐度 |
|------|--------|------|--------|
| **A1. Supabase** | ¥0 | 中等 | ⭐⭐⭐⭐⭐ 推荐 |
| A2. 阿里云 RDS | ¥50+ | 低 | ⭐⭐⭐⭐ |
| A3. Neon | ¥0 | 中等 | ⭐⭐⭐⭐ |

### ❓ 问题 3：数据迁移方式？

| 方式 | 难度 | 说明 |
|------|------|------|
| **CSV 导入（推荐）** | 简单 | 管理后台导出 → Supabase 导入 |
| SQL 导出导入 | 中等 | 适合数据量大的情况 |

---

## 十六、快速决策流程图

```
开始迁移？
    │
    ├── 是 → 选择计算平台
    │           │
    │           └── 推荐：阿里云 FC
    │
    └── 选择数据库
                │
                ├── 追求零成本 → Supabase ✅
                │
                └── 需要最低延迟 → 阿里云 RDS
```

---

## 十七、预期效果

### 迁移后对比

| 指标 | 迁移前（扣子） | 迁移后（FC+Supabase） |
|------|----------------|----------------------|
| 月成本 | ¥460+ | **¥0** |
| 年成本 | ¥5520+ | **¥0** |
| 国内访问 | 中等 | ⚡ 快 |
| 功能完整度 | 100% | **100%** |
| 用户门槛 | 无 | **无** |
| 维护成本 | 低 | **低** |

### 预期延迟分析

```
迁移前（扣子托管）：
用户 → 扣子平台（海外）→ 扣子数据库（海外）
总延迟：200-500ms

迁移后（FC + Supabase）：
用户 → FC（国内）→ Supabase（海外）
总延迟：100-400ms

结论：延迟可能略有改善或持平，不会变差！
```

---

**需要我帮你开始吗？**

确认后我可以：
1. 帮你创建 FC 配置文件
2. 生成数据库迁移脚本
3. 适配入口代码
4. 提供详细的操作步骤

---

*文档更新时间：2026-03-28*
