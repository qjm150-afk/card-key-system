# 卡密验证系统开发计划

> 创建时间：2026-03-14
> 最后更新：2026-03-17
> **项目状态：🟡 持续迭代**

---

## 一、紧急事项 🔴

> 需要立即处理，优先级最高

| 序号 | 事项 | 说明 | 状态 |
|-----|------|------|------|
| 1 | 执行数据备份 | `python scripts/backup_data.py` | ✅ 已完成 |
| 2 | 导出飞书多维表格数据 | 备份飞书侧数据 | ⏳ 待执行 |
| 3 | 记录所有飞书链接 | 整理系统中所有飞书链接清单 | ⏳ 待执行 |

---

## 二、高优先级功能 🟠

### 2.1 统计报表 ✅

**需求描述**：为管理后台添加数据统计报表功能

**功能要点**：
- [x] 卡密使用趋势图（按日/周/月）
- [x] 销售状态分布饼图
- [x] 设备绑定统计
- [x] 访问量统计
- [x] 导出报表功能

**状态**：✅ 已完成

---

### 2.2 到期提醒 ✅

**需求描述**：卡密即将过期时自动提醒管理员

**功能要点**：
- [x] 后台首页显示即将过期卡密列表（7天内/30天内）
- [x] 支持配置提醒阈值
- [x] 邮件/站内提醒（可选）
- [x] 批量延期操作

**状态**：✅ 已完成

---

### 2.3 导出增强 ✅

**需求描述**：增强卡密数据的导出功能

**功能要点**：
- [x] 支持导出为 Excel 格式
- [x] 支持自定义导出字段
- [x] 支持导出筛选结果
- [x] 支持导出访问日志
- [x] 导出文件命名优化（含日期时间）

**状态**：✅ 已完成

---

### 2.4 链接健康检测 ✅

**需求描述**：检测飞书链接是否有效

**功能要点**：
- [x] 定期检测所有飞书链接可访问性
- [x] 标记失效链接
- [x] 后台显示链接状态
- [x] 失效链接提醒

**状态**：✅ 已完成

---

### 2.5 卡密视图隔离 🆕

> **需求来源**：用户反馈
> **创建时间**：2026-03-17
> **优先级**：高
> **状态**：⏳ 规划中

#### 2.5.1 需求背景

用户当前有：
- 199 条用户卡密（阿奇索自动发货）
- 2 条内部使用卡密
- 需要区分手动发货和自动发货的卡密
- 需要查看飞书链接相同但备注/链接名称不同的卡密

**核心痛点**：在卡密管理页面中操作时，容易误操作，需要独立的视图来隔离特定条件的卡密。

#### 2.5.2 视图规划

| 视图名称 | 筛选条件 | 用途 |
|---------|---------|------|
| 内部卡密 | `card_type = 'internal'` | 管理内部使用的卡密 |
| 手动发货 | `delivery_method = 'manual'` | 管理手动发货的卡密 |
| 阿奇索发货 | `delivery_method = 'achiso'` | 管理阿奇索自动发货的卡密 |
| 链接分组 | 按 `feishu_url` 分组 | 查看同一链接的不同卡密配置 |

#### 2.5.3 数据库字段新增

**新增字段**：

| 字段名 | 类型 | 说明 | 可选值 |
|-------|------|------|--------|
| `card_type` | VARCHAR(20) | 卡密类型 | `external`(对外销售), `internal`(内部使用) |
| `delivery_method` | VARCHAR(20) | 发货方式 | `auto`(阿奇索自动), `manual`(手动发货) |

**默认值设置**：
- `card_type` 默认 `external`（对外销售）
- `delivery_method` 默认 `manual`（手动发货）

#### 2.5.4 前端标签页设计

```
┌─────────────────────────────────────────────────────────────────────┐
│ [卡密管理] [内部卡密] [手动发货] [阿奇索发货] [链接分组]  [+ 新建视图] │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 当前视图：内部卡密 (2条)                    [筛选] [导出]    │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ 序号 | 卡密 | 过期时间 | 备注 | 链接名称 | 状态 | 操作      │   │
│  │------|------|----------|------|----------|------|---------- │   │
│  │  1   | XXX  | 2027-03  | 测试 | 内部文档 | 有效 | [编辑]... │   │
│  │  2   | YYY  | 永久     | 备用 | 内部文档 | 有效 | [编辑]... │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

#### 2.5.5 视图操作能力

每个视图具备与卡密管理页面相同的操作能力：

| 操作 | 说明 |
|-----|------|
| 查看 | 浏览该视图下的卡密列表 |
| 编辑 | 修改单个卡密信息 |
| 批量操作 | 批量激活/停用/删除/修改链接 |
| 导出 | 导出该视图的卡密数据 |
| 筛选 | 在视图内进一步筛选 |

#### 2.5.6 开发任务拆解

**后端开发**：
- [ ] 数据库迁移：新增 `card_type` 和 `delivery_method` 字段
- [ ] API：新增字段支持（创建、编辑、批量生成、导入）
- [ ] API：新增视图数据接口（按条件筛选）
- [ ] API：链接分组统计接口

**前端开发**：
- [ ] 标签页组件开发
- [ ] 视图路由设计
- [ ] 各视图列表页开发
- [ ] 创建卡密弹窗新增字段选择
- [ ] 批量生成弹窗新增字段选择
- [ ] 编辑弹窗新增字段选择

#### 2.5.7 数据迁移策略

对于已有的 201 条卡密数据：

| 卡密类型 | 建议处理 |
|---------|---------|
| 199 条用户卡密 | `card_type=external`, `delivery_method=achiso` |
| 2 条内部卡密 | `card_type=internal`, `delivery_method=manual` |

**迁移方式**：
1. 提供数据迁移脚本
2. 或者在管理后台提供批量修改功能

#### 2.5.8 扩展性设计

**自定义视图（未来规划）**：
- 用户可保存筛选条件为自定义视图
- 视图支持自定义名称和图标
- 视图支持排序和显示/隐藏

---

## 三、中优先级功能 🟡

| 功能 | 说明 | 状态 |
|-----|------|------|
| 用户在线状态精准判断 | 改用心跳机制或WebSocket替代5分钟时间窗口 | ⏳ 待评估 |
| 飞书域名白名单 | 联系飞书管理员加入嵌入信任域名 | ⏳ 待跟进 |
| 飞书版本更新监测 | 关注飞书多维表格更新日志 | ⏳ 持续 |

---

## 四、飞书API集成 - 链接泄露检测 🔵

> **需求来源**：用户反馈
> **创建时间**：2026-03-16
> **优先级**：中高
> **状态**：⏳ 规划中

### 4.1 功能概述

通过集成飞书开放平台API，获取多维表格/云文档的访问统计数据，实现以下目标：

1. **泄露检测**：判断飞书链接是否被非法传播
2. **访问分析**：了解用户访问模式，优化运营策略
3. **安全预警**：自动检测异常访问行为

### 4.2 核心功能设计

#### 4.2.1 数据采集

| 数据项 | API来源 | 用途 |
|-------|---------|------|
| 每日访问人数 | 飞书统计API | 趋势分析 |
| 访问来源分布 | 飞书统计API | 内嵌 vs 外部访问判断 |
| 访问设备类型 | 飞书统计API | PC/移动端分布 |
| 访问地域分布 | 飞书统计API | 地域异常检测 |
| 分享/导出次数 | 飞书统计API | 泄露风险评估 |

#### 4.2.2 泄露检测逻辑

```
泄露风险评分 = f(外部访问比例, 异常访问时段, 地域分散度, 分享次数)

风险等级：
- 低风险 (0-30分)：外部访问比例 < 10%，无异常行为
- 中风险 (31-70分)：外部访问比例 10-30%，或存在少量异常
- 高风险 (71-100分)：外部访问比例 > 30%，或明显异常行为

预警触发条件：
1. 外部访问比例突增（较前日增长 > 50%）
2. 单日外部访问人数超过卡密有效数量
3. 非预期时段（如深夜）大量访问
4. 新地域突然出现大量访问
```

#### 4.2.3 功能界面设计

**管理后台 - 链接安全中心**

```
┌─────────────────────────────────────────────────────────────┐
│ 🔗 链接安全中心                                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ⚠️ 泄露预警 (2条)                          [查看详情]    │ │
│ │ • 链接「春招信息表」外部访问占比 45%，建议检查           │ │
│ │ • 链接「测试卡密」昨日外部访问突增 200%                 │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 链接名称         │ 内嵌访问 │ 外部访问 │ 泄露风险 │ 操作 │ │
│ ├─────────────────────────────────────────────────────────┤ │
│ │ 春招信息表       │ 120(55%) │ 98(45%)  │ 🔴 高    │ 详情 │ │
│ │ 秋招信息表       │ 200(95%) │ 10(5%)   │ 🟢 低    │ 详情 │ │
│ │ Sophie测试卡密   │ 50(80%)  │ 12(20%)  │ 🟡 中    │ 详情 │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ [同步飞书数据] [导出安全报告] [配置预警规则]                  │
└─────────────────────────────────────────────────────────────┘
```

**链接详情 - 访问趋势图**

```
┌─────────────────────────────────────────────────────────────┐
│ 「春招信息表」访问趋势 (近7天)                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  访问人数                                                   │
│  200 ┤                                                     │
│  150 ┤        ██                                           │
│  100 ┤   ██   ██  ██                                       │
│   50 ┤██ ██  ██  ██  ██                                    │
│    0 ┼──────────────────                                    │
│      03/10 03/11 03/12 03/13 03/14 03/15 03/16            │
│                                                             │
│  ─━ 内嵌访问  ── 外部访问                                    │
│                                                             │
│ 💡 外部访问占比：45%（高于安全阈值30%）                       │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 技术方案

#### 4.3.1 飞书API能力调研

**飞书开放平台相关API**：

| API | 能力 | 权限要求 | 调用限制 |
|-----|------|---------|---------|
| `bitable:record:read` | 读取多维表格记录 | 应用授权 | - |
| `drive:drive:readonly` | 读取云文档信息 | 应用授权 | - |
| `bitable:app:readonly` | 读取多维表格应用 | 应用授权 | - |

**个人版/免费版限制**：
- ⚠️ 需确认：飞书个人版是否支持开放平台API调用
- ⚠️ 需确认：免费版API调用频率限制（通常 100次/分钟）
- ⚠️ 需确认：统计数据API是否对个人版开放

**调研结论**：待确认

#### 4.3.2 系统架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                      卡密验证系统                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ 飞书链接管理 │───▶│ API配置管理 │───▶│ 数据采集服务 │     │
│  └─────────────┘    └─────────────┘    └──────┬──────┘     │
│                                                │            │
│                                                ▼            │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ 预警通知服务 │◀───│ 风险评估引擎 │◀───│ 统计数据存储 │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│         │                                                   │
│         ▼                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │               管理后台 - 链接安全中心                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 4.3.3 数据库设计

**新增表：feishu_link_stats（飞书链接统计）**

```sql
CREATE TABLE feishu_link_stats (
    id SERIAL PRIMARY KEY,
    feishu_url VARCHAR(500) NOT NULL,           -- 飞书链接
    link_name VARCHAR(200),                      -- 链接名称
    stat_date DATE NOT NULL,                     -- 统计日期
    
    -- 访问统计
    total_visitors INT DEFAULT 0,               -- 总访问人数
    embed_visitors INT DEFAULT 0,               -- 内嵌访问人数
    external_visitors INT DEFAULT 0,            -- 外部访问人数
    
    -- 设备分布
    pc_visitors INT DEFAULT 0,                  -- PC端访问
    mobile_visitors INT DEFAULT 0,              -- 移动端访问
    
    -- 其他指标
    share_count INT DEFAULT 0,                  -- 分享次数
    export_count INT DEFAULT 0,                 -- 导出次数
    
    -- 风险评估
    leak_risk_score INT DEFAULT 0,              -- 泄露风险评分(0-100)
    risk_level VARCHAR(20),                     -- 风险等级(low/medium/high)
    
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    
    UNIQUE(feishu_url, stat_date)               -- 每个链接每天一条记录
);
```

**新增表：feishu_api_config（飞书API配置）**

```sql
CREATE TABLE feishu_api_config (
    id SERIAL PRIMARY KEY,
    app_id VARCHAR(100) NOT NULL,               -- 飞书应用ID
    app_secret VARCHAR(200) NOT NULL,           -- 飞书应用密钥（加密存储）
    tenant_token VARCHAR(500),                  -- 租户访问令牌
    token_expire_at TIMESTAMP,                  -- 令牌过期时间
    
    -- 配置
    sync_enabled BOOLEAN DEFAULT true,          -- 是否启用同步
    sync_interval INT DEFAULT 86400,            -- 同步间隔(秒)，默认每天
    alert_threshold INT DEFAULT 30,             -- 预警阈值(外部访问比例%)
    
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

**新增表：leak_alerts（泄露预警记录）**

```sql
CREATE TABLE leak_alerts (
    id SERIAL PRIMARY KEY,
    feishu_url VARCHAR(500) NOT NULL,
    link_name VARCHAR(200),
    alert_type VARCHAR(50),                     -- 预警类型
    alert_level VARCHAR(20),                    -- 预警级别
    alert_message TEXT,                         -- 预警信息
    trigger_data JSONB,                         -- 触发数据
    
    status VARCHAR(20) DEFAULT 'pending',       -- pending/handled/ignored
    handled_at TIMESTAMP,
    handled_by VARCHAR(100),
    handle_note TEXT,
    
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### 4.3.4 API接口设计

**管理后台接口**

```
# 链接安全中心
GET  /api/admin/link-security/overview           # 安全概览
GET  /api/admin/link-security/links              # 链接安全列表
GET  /api/admin/link-security/links/:url/stats   # 单个链接统计详情
GET  /api/admin/link-security/alerts             # 预警列表
POST /api/admin/link-security/alerts/:id/handle  # 处理预警

# 飞书API配置
GET  /api/admin/feishu-api/config                # 获取API配置
POST /api/admin/feishu-api/config                # 保存API配置
POST /api/admin/feishu-api/sync                  # 手动同步数据
POST /api/admin/feishu-api/test                  # 测试API连接

# 预警规则配置
GET  /api/admin/alert-rules                      # 获取预警规则
POST /api/admin/alert-rules                      # 保存预警规则
```

### 4.4 开发任务拆解

#### Phase 1: 调研与准备（1-2天）

| 任务 | 说明 | 状态 |
|-----|------|------|
| 飞书开放平台账号注册 | 注册开发者账号，创建应用 | ⏳ 待执行 |
| API能力验证 | 确认统计数据API是否可用 | ⏳ 待执行 |
| 个人版限制确认 | 确认免费版调用限制 | ⏳ 待执行 |
| 技术方案最终确认 | 根据调研结果调整方案 | ⏳ 待执行 |

#### Phase 2: 核心功能开发（3-5天）

| 任务 | 说明 | 状态 |
|-----|------|------|
| 数据库表创建 | 创建统计、配置、预警表 | ⏳ 待开发 |
| 飞书API集成 | 实现API调用、Token管理 | ⏳ 待开发 |
| 数据采集服务 | 定时拉取统计数据 | ⏳ 待开发 |
| 风险评估引擎 | 实现泄露风险计算逻辑 | ⏳ 待开发 |
| 预警通知服务 | 实现预警触发和通知 | ⏳ 待开发 |

#### Phase 3: 管理后台开发（2-3天）

| 任务 | 说明 | 状态 |
|-----|------|------|
| 链接安全中心页面 | 安全概览、链接列表 | ⏳ 待开发 |
| 链接详情页 | 访问趋势图、详细数据 | ⏳ 待开发 |
| 预警管理页面 | 预警列表、处理流程 | ⏳ 待开发 |
| API配置页面 | 飞书API配置管理 | ⏳ 待开发 |

#### Phase 4: 测试与优化（1-2天）

| 任务 | 说明 | 状态 |
|-----|------|------|
| 功能测试 | 各模块功能验证 | ⏳ 待执行 |
| 性能测试 | API调用频率限制测试 | ⏳ 待执行 |
| 边界测试 | 异常情况处理 | ⏳ 待执行 |

### 4.5 风险与应对

| 风险 | 影响 | 应对措施 |
|-----|------|---------|
| 飞书个人版不支持统计API | 功能无法实现 | 考虑升级企业版，或使用替代方案（如自建访问统计） |
| API调用频率限制 | 数据同步延迟 | 增量同步、批量查询、本地缓存 |
| 统计数据延迟 | 预警不及时 | 设置合理预期，增加其他检测维度 |
| Token过期处理 | 服务中断 | 自动刷新机制 + 提前预警 |

### 4.6 替代方案

如果飞书API无法满足需求，考虑以下替代方案：

1. **自建访问统计**
   - 在iframe加载时记录访问日志
   - 通过Referer判断内嵌/外部访问
   - 局限：只能统计本系统内的访问，无法检测直接访问飞书链接的情况

2. **第三方统计工具**
   - 集成百度统计/Google Analytics
   - 局限：同样只能统计本系统内访问

3. **飞书企业版升级**
   - 获取完整API能力
   - 局限：增加成本

### 4.7 后续扩展方向

- 多维表格字段级权限监控
- 访问用户身份分析（如可能）
- 自动化风险处置（如检测到泄露自动重置链接）
- 泄露溯源追踪

---

## 五、低优先级功能 🟢

| 功能 | 说明 | 状态 |
|-----|------|------|
| 飞书开放平台方案 | 关注云文档组件嵌入方案 | ⏳ 长期关注 |
| 官方嵌入方式评估 | 评估是否切换到官方推荐方式 | ⏳ 长期关注 |

---

## 六、功能详细规划

### 5.1 统计报表详细设计

#### 数据指标

| 指标名称 | 计算方式 | 展示形式 |
|---------|---------|---------|
| 总卡密数 | COUNT(*) | 数字卡片 |
| 有效卡密 | COUNT(status=1) | 数字卡片 |
| 已过期卡密 | COUNT(expire_time < now) | 数字卡片 |
| 即将过期(7天) | COUNT(expire_time < now+7d) | 数字卡片 + 列表 |
| 今日验证次数 | COUNT(access_logs today) | 数字卡片 |
| 本周新增卡密 | COUNT(created this week) | 趋势图 |
| 销售状态分布 | GROUP BY sales_status | 饼图 |
| 设备绑定分布 | GROUP BY device_count | 柱状图 |

#### API 接口

```
GET /api/admin/statistics/overview     # 总览数据
GET /api/admin/statistics/trend        # 趋势数据（按日/周/月）
GET /api/admin/statistics/distribution # 分布数据
```

---

### 5.2 到期提醒详细设计

#### 后台首页展示

```
┌─────────────────────────────────────┐
│ ⚠️ 即将过期提醒                      │
├─────────────────────────────────────┤
│ 7天内过期: 12 张卡密  [查看详情]      │
│ 30天内过期: 45 张卡密 [查看详情]      │
└─────────────────────────────────────┘
```

#### 功能配置

| 配置项 | 默认值 | 说明 |
|-------|-------|------|
| 提醒阈值 | 7天 | 提前多少天开始提醒 |
| 首页显示数量 | 10条 | 最多显示多少条即将过期卡密 |
| 邮件提醒开关 | 关闭 | 是否发送邮件提醒 |

---

### 5.3 导出增强详细设计

#### 导出格式支持

| 格式 | 说明 |
|-----|------|
| CSV | 默认格式，兼容性好 |
| Excel (.xlsx) | 带格式，支持多sheet |
| JSON | 便于程序处理 |

#### 导出字段选择

```
可选字段：
☑ 卡密
☑ 激活状态
☑ 销售状态
☑ 过期时间
☑ 绑定设备数
☐ 飞书链接
☐ 访问密码
☑ 备注
☑ 创建时间
☐ 访问日志
```

---

### 5.4 链接健康检测详细设计

#### 检测逻辑

```
1. 定时任务：每天凌晨检测一次
2. 手动触发：后台按钮立即检测
3. 检测方式：HEAD请求飞书链接，检查HTTP状态码
4. 状态标记：
   - 正常 (200)
   - 失效 (404/403/500等)
   - 未知 (超时/网络错误)
```

#### 后台展示

```
┌─────────────────────────────────────┐
│ 🔗 链接健康状态                       │
├─────────────────────────────────────┤
│ 正常: 15 个                          │
│ 失效: 2 个   [查看详情]              │
│ 未知: 0 个                          │
│ [立即检测]                           │
└─────────────────────────────────────┘
```

---

## 七、开发进度记录

| 日期 | 完成内容 | 备注 |
|------|---------|------|
| 2026-03-14 | 创建开发计划文档 | - |

---

## 八、变更记录

| 日期 | 变更内容 | 变更人 |
|------|---------|--------|
| 2026-03-14 | 初始化开发计划 | - |
| 2026-03-16 | 新增「飞书API集成 - 链接泄露检测」功能规划 | - |
| 2026-03-17 | 新增「卡密视图隔离」功能规划，包含内部卡密、手动发货、阿奇索发货、链接分组视图 | - |
